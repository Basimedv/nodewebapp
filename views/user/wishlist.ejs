<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>COLINGUEST - My Wishlist</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --success-gradient: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            --danger-gradient: linear-gradient(135deg, #dc3545 0%, #fd7e14 100%);
            --shadow-lg: 0 10px 30px rgba(0, 0, 0, 0.1);
            --shadow-sm: 0 2px 10px rgba(0, 0, 0, 0.05);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        .swal2-container {
            z-index: 99999 !important;
        }

        .swal2-popup {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif !important;
        }

        .wishlist-container {
            max-width: 1400px;
            margin: 2rem auto;
            padding: 0 2rem;
        }

        .wishlist-header {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            margin-bottom: 2rem;
            box-shadow: var(--shadow-sm);
        }

        .wishlist-header h2 {
            margin: 0;
            color: #2c3e50;
            font-weight: 600;
        }

        .wishlist-header p {
            margin: 0.5rem 0 0 0;
            color: #6c757d;
        }

        .empty-wishlist {
            background: white;
            border-radius: 20px;
            padding: 4rem 2rem;
            text-align: center;
            box-shadow: var(--shadow-lg);
        }

        .empty-wishlist i {
            font-size: 4rem;
            color: #e9ecef;
            margin-bottom: 1.5rem;
        }

        .empty-wishlist h3 {
            color: #6c757d;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .empty-wishlist p {
            color: #adb5bd;
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        .continue-shopping {
            background: var(--primary-gradient);
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 25px;
            text-decoration: none;
            display: inline-block;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .continue-shopping:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }

        .wishlist-item {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: var(--shadow-sm);
            border: 1px solid #e9ecef;
            transition: all 0.3s ease;
        }

        .wishlist-item:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-lg);
            border-color: #667eea;
        }

        .wishlist-item-image {
            width: 120px;
            height: 150px;
            object-fit: cover;
            border-radius: 10px;
            border: 2px solid #f8f9fa;
            background-color: #f8f9fa;
        }

        .wishlist-item-details {
            flex: 1;
            padding-left: 1.5rem;
        }

        .wishlist-item-title {
            font-size: 1.2rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: #2c3e50;
            line-height: 1.4;
        }

        .wishlist-item-price {
            font-size: 1.1rem;
            color: #6c757d;
            margin-bottom: 1rem;
        }

        .wishlist-item-original-price {
            text-decoration: line-through;
            color: #adb5bd;
            margin-right: 0.5rem;
        }

        .wishlist-item-actions {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }

        .add-to-cart-btn {
            background: var(--success-gradient);
            color: white;
            border: none;
            padding: 0.7rem 1.5rem;
            border-radius: 25px;
            cursor: pointer;
            font-size: 0.9rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .add-to-cart-btn:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.3);
        }

        .add-to-cart-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .remove-btn {
            background: var(--danger-gradient);
            color: white;
            border: none;
            padding: 0.7rem 1.5rem;
            border-radius: 25px;
            cursor: pointer;
            font-size: 0.9rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .remove-btn:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(220, 53, 69, 0.3);
        }

        .remove-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .spinner-border-sm {
            width: 1rem;
            height: 1rem;
            border-width: 0.15em;
        }

        @media (max-width: 768px) {
            .wishlist-container {
                padding: 0 1rem;
            }

            .wishlist-item {
                flex-direction: column;
                text-align: center;
            }

            .wishlist-item-image {
                width: 100%;
                height: 200px;
                margin-bottom: 1rem;
            }

            .wishlist-item-details {
                padding-left: 0;
            }

            .wishlist-item-actions {
                justify-content: center;
                flex-wrap: wrap;
            }
        }
    </style>
</head>

<body data-user-id="<%= user ? user._id : '' %>">
   
  
   <%- include("../partials/user/header") %>
   
    <div class="wishlist-container">
        <% if (typeof items !== 'undefined' && items.length > 0) { %>
            <div class="wishlist-header">
                <h2><i class="fas fa-heart me-2 text-danger"></i>My Wishlist (<span id="wishlist-count"><%= items.length %></span> items)</h2>
                <p>Your saved items for later</p>
            </div>

            <div class="row">
                <div class="col-12" id="wishlist-items-container">
                    <% items.forEach((item) => { %>
                        <% const product = item.product; %>
                        <% if (product && !product.isDeleted) { %>
                        <div class="wishlist-item d-flex" data-product-id="<%= product._id %>">
                            <% 
                            let imageUrl = 'https://via.placeholder.com/120x150?text=No+Image';
                            if (product.productImage && Array.isArray(product.productImage) && product.productImage.length > 0) {
                                imageUrl = product.productImage[0];
                            } else if (product.images && Array.isArray(product.images) && product.images.length > 0) {
                                imageUrl = product.images[0];
                            }
                            %>
                            <img src="<%= imageUrl %>"
                                 alt="<%= product.productName || product.name || 'Product' %>"
                                 class="wishlist-item-image"
                                 onerror="this.onerror=null; this.src='https://via.placeholder.com/120x150?text=No+Image';"
                                 loading="lazy">

                            <div class="wishlist-item-details">
                                <div class="wishlist-item-title">
                                    <%= product.name || product.productName || 'Product Name' %>
                                </div>

                                <div class="wishlist-item-price">
                                    <% 
                                    const regularPrice = product.regularPrice || product.price || 0;
                                    const salePrice = product.salePrice || 0;
                                    const offerPercent = product.offerPercent || 0;
                                    const finalPrice = (salePrice > 0 && salePrice < regularPrice) 
                                        ? salePrice 
                                        : (offerPercent > 0 ? Math.round(regularPrice - (regularPrice * offerPercent / 100)) : regularPrice);
                                    %>

                                    <% if (finalPrice < regularPrice) { %>
                                        <span class="wishlist-item-original-price">₹<%= regularPrice.toLocaleString('en-IN') %></span>
                                        <span class="text-success fw-bold">₹<%= finalPrice.toLocaleString('en-IN') %></span>
                                        <% if (offerPercent > 0) { %>
                                            <span class="badge bg-success ms-1"><%= offerPercent %>% OFF</span>
                                        <% } %>
                                    <% } else { %>
                                        <span class="fw-bold">₹<%= regularPrice.toLocaleString('en-IN') %></span>
                                    <% } %>
                                </div>

                                <div class="wishlist-item-actions">
                                    <button class="add-to-cart-btn" onclick="addToCart('<%= product._id %>', this)">
                                        <i class="fas fa-shopping-cart me-1"></i>Add to Cart
                                    </button>
                                    <button class="remove-btn" onclick="removeFromWishlist('<%= product._id %>', this)">
                                        <i class="fas fa-trash me-1"></i>Remove
                                    </button>
                                </div>
                            </div>
                        </div>
                        <% } %>
                    <% }) %>
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="/shop" class="btn btn-outline-secondary btn-lg">
                    <i class="fas fa-arrow-left me-2"></i>Continue Shopping
                </a>
            </div>
        <% } else { %>
            <div class="empty-wishlist">
                <i class="fas fa-heart"></i>
                <h3>Your Wishlist is Empty</h3>
                <p>Save your favorite items here for later!</p>
                <a href="/shop" class="continue-shopping">
                    <i class="fas fa-store me-2"></i>Start Shopping
                </a>
            </div>
        <% } %>
    </div>

    <%- include("../partials/user/footer") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        const userId = document.body.dataset.userId;

        async function addToCart(productId, buttonElement) {
            if (!userId) {
                await Swal.fire({
                    icon: 'warning',
                    title: 'Login Required',
                    text: 'Please login to add items to cart',
                    confirmButtonText: 'Go to Login',
                    showCancelButton: true,
                    cancelButtonText: 'Cancel',
                    position: 'center'
                }).then((result) => {
                    if (result.isConfirmed) {
                        window.location.href = '/login';
                    }
                });
                return;
            }

            buttonElement.disabled = true;
            const originalHTML = buttonElement.innerHTML;
            buttonElement.innerHTML = '<span class="spinner-border spinner-border-sm me-1"></span>Adding...';

            setTimeout(() => {
                window.location.href = `/productDetails/${productId}`;
            }, 300);
        }

        async function removeFromWishlist(productId, buttonElement) {
            if (!userId) {
                await Swal.fire({
                    icon: 'warning',
                    title: 'Login Required',
                    text: 'Please login to manage your wishlist',
                    position: 'center'
                });
                return;
            }

            const result = await Swal.fire({
                title: 'Remove from Wishlist?',
                text: 'Are you sure you want to remove this item?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Yes, Remove',
                cancelButtonText: 'Cancel',
                confirmButtonColor: '#dc3545',
                cancelButtonColor: '#6c757d',
                position: 'center'
            });

            if (!result.isConfirmed) return;

            buttonElement.disabled = true;
            const originalHTML = buttonElement.innerHTML;
            buttonElement.innerHTML = '<span class="spinner-border spinner-border-sm"></span>';

            try {
                console.log('🗑️ Removing product:', productId);

                // Use addToWishlist endpoint (it toggles - removes if already in wishlist)
                const response = await fetch('/user/addToWishlist', {
                    method: 'POST',
                    headers: { 
                        'Content-Type': 'application/json',
                        'Accept': 'application/json'
                    },
                    body: JSON.stringify({ 
                        userId: userId,
                        productId: productId 
                    })
                });

                console.log('Response status:', response.status);
                console.log('Response headers:', response.headers.get('content-type'));

                if (!response.ok) {
                    const errorText = await response.text();
                    console.error('Error response:', errorText);
                    throw new Error(`Server error: ${response.status}`);
                }

                const contentType = response.headers.get('content-type');
                if (!contentType || !contentType.includes('application/json')) {
                    const htmlText = await response.text();
                    console.error('Received HTML instead of JSON:', htmlText.substring(0, 200));
                    throw new Error('Server returned HTML instead of JSON. Check your route and controller.');
                }

                const data = await response.json();
                console.log('Response data:', data);

                if (data.success && data.action === 'removed') {
                    await Swal.fire({
                        icon: 'success',
                        title: 'Removed!',
                        text: 'Item removed from wishlist',
                        timer: 1500,
                        showConfirmButton: false,
                        position: 'center'
                    });

                    const itemElement = document.querySelector(`[data-product-id="${productId}"]`);
                    if (itemElement) {
                        itemElement.style.transition = 'all 0.3s ease';
                        itemElement.style.transform = 'translateX(-100%)';
                        itemElement.style.opacity = '0';

                        setTimeout(() => {
                            itemElement.remove();
                            
                            const remainingItems = document.querySelectorAll('.wishlist-item');
                            const countElement = document.getElementById('wishlist-count');
                            
                            if (countElement) {
                                countElement.textContent = remainingItems.length;
                            }

                            if (remainingItems.length === 0) {
                                window.location.reload();
                            }
                        }, 300);
                    }
                } else {
                    throw new Error(data.error || data.message || 'Failed to remove item');
                }
            } catch (error) {
                console.error('❌ Remove error:', error);
                buttonElement.disabled = false;
                buttonElement.innerHTML = originalHTML;
                
                await Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: error.message || 'Failed to remove item. Please try again.',
                    position: 'center'
                });
            }
        }
    </script>
</body>
</html>