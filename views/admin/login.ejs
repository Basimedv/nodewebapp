<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-image: url(/images/background.jpg);
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        main {
            background: white;
            border-radius: 20px;
            width: 400px;
            max-width: 90%;
            padding: 40px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        main:hover {
            transform: translateY(-5px);
        }

        .adminDetails {
            width: 100%;
        }

        .mainHead {
            margin-bottom: 30px;
            position: relative;
            text-align: center;
        }

        .mainHead img {
            width: 40px;
            height: 40px;
        }

        .mainHead p {
            font-size: 26px;
            font-weight: 600;
            color: #333;
            margin-bottom: 5px;
        }

        form {
            text-align: left;
        }

        label {
            display: block;
            color: #555;
            margin-bottom: 8px;
            font-size: 14px;
            font-weight: 500;
        }

        input {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            transition: all 0.3s ease;
            margin-bottom: 5px;
        }

        input:focus {
            border-color: #6c63ff;
            box-shadow: 0 0 0 2px rgba(108, 99, 255, 0.2);
            outline: none;
        }

        /* Password wrapper styles */
        .password-wrapper {
            position: relative;
            margin-bottom: 5px;
        }

        .password-wrapper input {
            padding-right: 45px;
        }

        .toggle-password {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #666;
            font-size: 18px;
            transition: color 0.3s;
        }

        .toggle-password:hover {
            color: #6c63ff;
        }

        button {
            width: 100%;
            background: linear-gradient(135deg, #6c63ff 0%, #4834d4 100%);
            color: white;
            border: none;
            padding: 12px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            margin-top: 15px;
            transition: all 0.3s ease;
        }

        button:hover {
            background: linear-gradient(135deg, #5a52d5 0%, #3b2bb8 100%);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(72, 52, 212, 0.3);
        }

        .error-message {
            color: #e74c3c;
            font-size: 14px;
            margin: 5px 0 15px;
        }
    </style>
</head>

<body>
    <main>
        <div class="adminDetails">
            <div class="mainHead">
                <img src="/images/colinguest.png" alt="Admin Logo">
                <p>Welcome</p>
            </div>

            <% if (error) { %>
                <p id="errorMsg" style="color:red;">
                    <%= error %>
                </p>
            <% } %>

            <form action="/admin/adminLogin" method="post">
                <label for="email">Email Address</label>
                <input type="email" name="email" id="email" placeholder="Enter your email" required>

                <label for="password">Password</label>
                <div class="password-wrapper">
                    <input type="password" name="password" id="password" placeholder="Enter your password" required>
                    <i class="far fa-eye-slash toggle-password" onclick="togglePassword('password', this)"></i>
                </div>

                <button type="submit">Sign In</button>
            </form>
        </div>
    </main>

    <script>
        // Password Toggle Function
        function togglePassword(inputId, icon) {
            const input = document.getElementById(inputId);
            if (input.type === 'password') {
                input.type = 'text';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            } else {
                input.type = 'password';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            }
        }

        // Error Message Auto Hide
        const errorMsg = document.getElementById('errorMsg');
        if (errorMsg) {
            setTimeout(() => {
                errorMsg.style.display = 'none';
            }, 3000);
        }
    </script>
</body>

</html>

<!-- 
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Admin Portal | Secure Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --ai-glow: #00d2ff;
            --ai-purple: #9d50bb;
            --dark-bg: #05070a;
            --card-bg: rgba(10, 15, 25, 0.9);
            --error-red: #ff4d4d;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: var(--dark-bg);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            color: white;
        }

        /* Neural Background Effect */
        .bg-animate {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 210, 255, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(157, 80, 187, 0.1) 0%, transparent 40%);
            z-index: -1;
            animation: pulseBg 10s infinite alternate;
        }

        @keyframes pulseBg {
            0% { transform: scale(1); opacity: 0.5; }
            100% { transform: scale(1.1); opacity: 1; }
        }

        /* Shimmering AI Border Container */
        .login-card {
            position: relative;
            padding: 2px;
            background: linear-gradient(45deg, var(--ai-glow), var(--ai-purple), var(--ai-glow));
            background-size: 200% 200%;
            border-radius: 24px;
            animation: borderRotate 4s linear infinite;
            box-shadow: 0 0 30px rgba(0, 210, 255, 0.15);
        }

        @keyframes borderRotate {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        main {
            background: var(--card-bg);
            backdrop-filter: blur(25px);
            border-radius: 22px;
            width: 400px;
            padding: 50px 40px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        /* Vertical Scan Line */
        .scan-line {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: linear-gradient(to right, transparent, var(--ai-glow), transparent);
            opacity: 0.5;
            animation: scan 4s linear infinite;
            pointer-events: none;
        }

        @keyframes scan {
            0% { top: 0; }
            100% { top: 100%; }
        }

        .mainHead img {
            width: 50px;
            filter: drop-shadow(0 0 10px var(--ai-glow));
            margin-bottom: 15px;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }

        h2 {
            font-size: 22px;
            font-weight: 500;
            margin-bottom: 30px;
            letter-spacing: 1px;
            background: linear-gradient(to right, #ffffff, #94a3b8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .input-group {
            position: relative;
            margin-bottom: 20px;
            text-align: left;
        }

        input {
            width: 100%;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 14px 16px;
            border-radius: 12px;
            color: white;
            outline: none;
            font-size: 14px;
            transition: all 0.3s;
        }

        input:focus {
            border-color: var(--ai-glow);
            background: rgba(255, 255, 255, 0.1);
            box-shadow: 0 0 15px rgba(0, 210, 255, 0.2);
        }

        /* Evasive Button Styling */
        #submitBtn {
            width: 100%;
            padding: 14px;
            border-radius: 12px;
            border: none;
            background: #ffffff;
            color: #000000;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s ease-out, background 0.3s, box-shadow 0.3s;
            margin-top: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            position: relative;
        }

        #submitBtn.jumping {
            background: var(--error-red);
            color: white;
            box-shadow: 0 0 20px rgba(255, 77, 77, 0.4);
        }

        #submitBtn:not(.jumping):hover {
            background: var(--ai-glow);
            color: white;
            box-shadow: 0 0 20px rgba(0, 210, 255, 0.4);
        }

        #errorMsg {
            color: var(--error-red);
            font-size: 13px;
            margin-bottom: 15px;
            display: block;
            background: rgba(255, 77, 77, 0.1);
            padding: 8px;
            border-radius: 8px;
            border: 1px solid rgba(255, 77, 77, 0.2);
        }

        .password-container {
            position: relative;
        }

        .toggle-password {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #64748b;
            transition: 0.3s;
        }

        .toggle-password:hover {
            color: var(--ai-glow);
        }
    </style>
</head>

<body>
    <div class="bg-animate"></div>

    <div class="login-card" id="cardContainer">
        <div class="scan-line"></div>
        <main>
            <div class="mainHead">
                <img src="/images/colinguest.png" alt="AI Admin">
                <h2>System Authentication</h2>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <span id="errorMsg"><i class="fas fa-fingerprint"></i> <%= error %></span>
            <% } %>

            <form action="/admin/adminLogin" method="post" id="loginForm">
                <div class="input-group">
                    <input type="email" name="email" id="email" placeholder="Admin Email" required>
                </div>

                <div class="input-group password-container">
                    <input type="password" name="password" id="password" placeholder="Access Key" required>
                    <i class="far fa-eye-slash toggle-password" onclick="togglePassword()"></i>
                </div>

                <button type="submit" id="submitBtn">
                    <span id="btnText">Initiate Session</span>
                    <i class="fas fa-bolt"></i>
                </button>
            </form>
        </main>
    </div>

    <script>
        const btn = document.getElementById('submitBtn');
        const email = document.getElementById('email');
        const password = document.getElementById('password');
        const form = document.getElementById('loginForm');

        // 1. THE EVASIVE BUTTON LOGIC
        btn.addEventListener('mouseover', () => {
            // Check if either field is empty
            if (email.value.trim() === "" || password.value.trim() === "") {
                btn.classList.add('jumping');
                
                // Calculate random jump position
                const x = Math.random() * 140 * (Math.random() < 0.5 ? -1 : 1);
                const y = Math.random() * 80 * (Math.random() < 0.5 ? -1 : 1);
                
                btn.style.transform = `translate(${x}px, ${y}px)`;
                document.getElementById('btnText').innerText = "Fill required fields!";
            }
        });

        // Reset button when user starts typing
        [email, password].forEach(el => {
            el.addEventListener('input', () => {
                btn.style.transform = `translate(0, 0)`;
                btn.classList.remove('jumping');
                document.getElementById('btnText').innerText = "Initiate Session";
            });
        });

        // 2. PASSWORD TOGGLE
        function togglePassword() {
            const passInput = document.getElementById('password');
            const icon = document.querySelector('.toggle-password');
            if (passInput.type === 'password') {
                passInput.type = 'text';
                icon.classList.replace('fa-eye-slash', 'fa-eye');
            } else {
                passInput.type = 'password';
                icon.classList.replace('fa-eye', 'fa-eye-slash');
            }
        }

        // 3. MOUSE-FOLLOW PARALLAX (Subtle AI Feel)
        document.addEventListener('mousemove', (e) => {
            const card = document.getElementById('cardContainer');
            const xAxis = (window.innerWidth / 2 - e.pageX) / 25;
            const yAxis = (window.innerHeight / 2 - e.pageY) / 25;
            card.style.transform = `rotateY(${xAxis}deg) rotateX(${yAxis}deg)`;
        });

        // 4. LOADING STATE ON SUCCESSFUL SUBMIT
        form.onsubmit = (e) => {
            // Check one last time before allowing submit
            if (email.value.trim() !== "" && password.value.trim() !== "") {
                btn.innerHTML = '<i class="fas fa-sync fa-spin"></i> Synchronizing...';
                btn.style.pointerEvents = 'none';
            } else {
                e.preventDefault();
            }
        };
    </script>
</body>

</html> -->